@model IEnumerable<WebApplication.Models.RequirementConfirmation>

<div class="modal-content">
    <div class="modal-header">
        <button class="close" data-dismiss="modal" area-hidden="true">X</button>
        <h4>Детали удаляемой заявки</h4>
    </div>
    <div class="modal-body">
        <table>
            <thead>
            <tr>
                <th></th>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Author)
                </th>
                <th>
                    Скачать подтверждение
                </th>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td></td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Author)
                    </td>
                    <td>
                        <a href="/Document/DownloadRequirementConfirmation/@item.Id"><img src="../../Content/pictures/filey.png" class="tdImage" title="Скачать подтверждение" /></a>
                    </td>
                </tr>
            }
            </tbody>
            
        </table>
    </div>

    <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">Закрыть</button>
    </div>
</div>